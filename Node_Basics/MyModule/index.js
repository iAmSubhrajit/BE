

const add = (a,b) => {
    console.log(`${a}+${b} = ${a+b}`);
}
const multiply = (a,b) => {
    console.log(`${a}*${b} = ${a*b}`);
}
const substract = (a,b) => {
    console.log(`${a}-${b} = ${a-b}`);
}

module.exports = {add,substract, multiply}